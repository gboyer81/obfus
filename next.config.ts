import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	turbopack: {
		// Set the root to the correct project directory
		root: '/Users/gary/dev/web3-dash',
	},
}

export default nextConfig;
